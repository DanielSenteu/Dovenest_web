{
  "name": "dovenest-insurance",
  "version": "1.0.0",
  "description": "DoveNest Insurance Brokers website + quote server",
  "private": true,
  "scripts": {
    "start": "node server/server.js",
    "test": "node --test",
    "build": "node scripts/build.js"
  },
  "main": "server/server.js",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pdfkit": "^0.18.0"
  }
}
